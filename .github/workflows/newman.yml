name: Newman Run

on:
  pull_request:
    branches:
    - germanking6-patch-1

jobs:
  newman:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: matt-ball/newman-action@master
        with:
          collection: https://www.getpostman.com/collections/ec65e83a23cc3a69d736
